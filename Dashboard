<!DOCTYPE html>
<html lang="it">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>WDA × PSN — Startup Scouting Decision Dashboard</title>
  <style>
    :root {
      --bg: #0b1020;
      --panel: #111936;
      --panel-2: #162044;
      --card: rgba(255,255,255,0.075);
      --card-strong: rgba(255,255,255,0.11);
      --text: #f6f7fb;
      --muted: #aeb7d8;
      --muted-2: #7f8ab2;
      --line: rgba(255,255,255,0.12);
      --blue: #7fb7ff;
      --cyan: #6ee7f9;
      --green: #7cf2b3;
      --yellow: #ffe08a;
      --orange: #ffb169;
      --red: #ff7d8a;
      --purple: #c8a7ff;
      --shadow: 0 24px 70px rgba(0,0,0,0.35);
      --radius: 24px;
    }

    * { box-sizing: border-box; }
    html { scroll-behavior: smooth; }
    body {
      margin: 0;
      font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      background:
        radial-gradient(circle at 10% 5%, rgba(111,231,249,0.20), transparent 28%),
        radial-gradient(circle at 88% 0%, rgba(200,167,255,0.22), transparent 30%),
        linear-gradient(180deg, #0b1020 0%, #091021 55%, #080b16 100%);
      color: var(--text);
      min-height: 100vh;
    }

    a { color: inherit; text-decoration: none; }

    .shell {
      width: min(1440px, calc(100% - 40px));
      margin: 0 auto;
      padding: 28px 0 72px;
    }

    .topbar {
      position: sticky;
      top: 0;
      z-index: 20;
      backdrop-filter: blur(18px);
      background: rgba(8, 12, 26, 0.72);
      border-bottom: 1px solid rgba(255,255,255,0.08);
    }

    .topbar-inner {
      width: min(1440px, calc(100% - 40px));
      margin: 0 auto;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 18px;
      padding: 14px 0;
    }

    .brand {
      display: flex;
      align-items: center;
      gap: 12px;
      min-width: 245px;
    }

    .brand-mark {
      width: 38px;
      height: 38px;
      border-radius: 13px;
      background: linear-gradient(135deg, var(--cyan), var(--purple));
      box-shadow: 0 12px 30px rgba(110,231,249,0.22);
      display: grid;
      place-items: center;
      color: #071024;
      font-weight: 900;
      letter-spacing: -0.08em;
    }

    .brand-title { line-height: 1.1; }
    .brand-title strong { display:block; font-size: 14px; letter-spacing: 0.02em; }
    .brand-title span { color: var(--muted); font-size: 12px; }

    .nav {
      display: flex;
      align-items: center;
      gap: 8px;
      flex-wrap: wrap;
      justify-content: flex-end;
    }

    .nav a,
    .button-like {
      border: 1px solid rgba(255,255,255,0.12);
      background: rgba(255,255,255,0.06);
      color: var(--muted);
      padding: 9px 12px;
      border-radius: 999px;
      font-size: 12px;
      transition: 0.2s ease;
      cursor: pointer;
    }

    .nav a:hover,
    .button-like:hover,
    .nav a.active {
      color: var(--text);
      background: rgba(127,183,255,0.14);
      border-color: rgba(127,183,255,0.28);
    }

    .hero {
      padding: 68px 0 38px;
      display: grid;
      grid-template-columns: minmax(0, 1.08fr) minmax(360px, 0.92fr);
      gap: 30px;
      align-items: stretch;
    }

    .hero-panel {
      border: 1px solid rgba(255,255,255,0.12);
      background: linear-gradient(145deg, rgba(255,255,255,0.10), rgba(255,255,255,0.045));
      border-radius: var(--radius);
      padding: 34px;
      box-shadow: var(--shadow);
      position: relative;
      overflow: hidden;
    }

    .hero-panel::after {
      content:"";
      position:absolute;
      right:-100px;
      top:-130px;
      width:300px;
      height:300px;
      border-radius:50%;
      background: radial-gradient(circle, rgba(110,231,249,0.28), transparent 68%);
      pointer-events:none;
    }

    .eyebrow {
      display:inline-flex;
      align-items:center;
      gap:8px;
      color: var(--cyan);
      border:1px solid rgba(110,231,249,0.22);
      background: rgba(110,231,249,0.08);
      padding: 8px 11px;
      border-radius:999px;
      font-size:12px;
      font-weight:700;
      letter-spacing:0.04em;
      text-transform: uppercase;
      margin-bottom: 20px;
    }

    h1 {
      font-size: clamp(42px, 6vw, 76px);
      line-height: 0.92;
      margin: 0;
      letter-spacing: -0.07em;
      max-width: 860px;
    }

    .hero-copy {
      color: var(--muted);
      font-size: 18px;
      line-height: 1.6;
      margin: 22px 0 0;
      max-width: 790px;
    }

    .hero-actions {
      display:flex;
      flex-wrap:wrap;
      gap: 12px;
      margin-top: 28px;
    }

    .primary-cta,
    .secondary-cta {
      border-radius: 999px;
      padding: 13px 18px;
      font-weight: 800;
      font-size: 13px;
      border: 1px solid transparent;
      transition: 0.2s ease;
    }

    .primary-cta {
      color: #071024;
      background: linear-gradient(135deg, var(--cyan), var(--green));
      box-shadow: 0 14px 36px rgba(110,231,249,0.22);
    }

    .secondary-cta {
      color: var(--text);
      background: rgba(255,255,255,0.065);
      border-color: rgba(255,255,255,0.12);
    }

    .hero-side {
      display:grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 14px;
    }

    .metric-card {
      border: 1px solid rgba(255,255,255,0.1);
      background: rgba(255,255,255,0.07);
      border-radius: 22px;
      padding: 22px;
      min-height: 145px;
      display:flex;
      flex-direction:column;
      justify-content:space-between;
    }

    .metric-card.big {
      grid-column: span 2;
      background: linear-gradient(135deg, rgba(127,183,255,0.16), rgba(200,167,255,0.12));
    }

    .metric-value {
      font-size: 46px;
      font-weight: 950;
      letter-spacing: -0.06em;
    }

    .metric-label {
      color: var(--muted);
      line-height: 1.35;
      font-size: 13px;
    }

    .metric-note {
      color: var(--muted-2);
      font-size: 12px;
      margin-top: 10px;
    }

    section { margin-top: 34px; }

    .section-head {
      display:flex;
      align-items:flex-end;
      justify-content:space-between;
      gap: 20px;
      margin: 0 0 16px;
    }

    .section-kicker {
      color: var(--cyan);
      font-size: 12px;
      letter-spacing: 0.08em;
      font-weight: 800;
      text-transform: uppercase;
      margin-bottom: 8px;
    }

    h2 {
      font-size: clamp(28px, 3vw, 42px);
      letter-spacing: -0.045em;
      margin: 0;
      line-height: 1.05;
    }

    .section-desc {
      color: var(--muted);
      max-width: 660px;
      line-height: 1.55;
      font-size: 15px;
      margin: 8px 0 0;
    }

    .panel {
      border: 1px solid rgba(255,255,255,0.11);
      background: rgba(255,255,255,0.06);
      border-radius: var(--radius);
      padding: 22px;
      box-shadow: 0 18px 55px rgba(0,0,0,0.20);
    }

    .filters {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
      margin: 18px 0 0;
    }

    .filter-btn {
      border: 1px solid rgba(255,255,255,0.12);
      color: var(--muted);
      background: rgba(255,255,255,0.055);
      padding: 10px 13px;
      border-radius: 999px;
      font-size: 13px;
      cursor:pointer;
      transition: 0.18s ease;
    }

    .filter-btn.active,
    .filter-btn:hover {
      color: #071024;
      background: linear-gradient(135deg, var(--cyan), var(--green));
      border-color: transparent;
      font-weight: 850;
    }

    .grid {
      display:grid;
      gap: 16px;
    }

    .grid.two { grid-template-columns: repeat(2, minmax(0, 1fr)); }
    .grid.three { grid-template-columns: repeat(3, minmax(0, 1fr)); }
    .grid.four { grid-template-columns: repeat(4, minmax(0, 1fr)); }

    .startup-grid {
      display:grid;
      grid-template-columns: repeat(4, minmax(0, 1fr));
      gap: 16px;
      margin-top: 18px;
    }

    .startup-card {
      border: 1px solid rgba(255,255,255,0.105);
      background: linear-gradient(180deg, rgba(255,255,255,0.088), rgba(255,255,255,0.045));
      border-radius: 22px;
      padding: 18px;
      min-height: 310px;
      display:flex;
      flex-direction: column;
      gap: 14px;
      position:relative;
      overflow:hidden;
      transition: transform 0.2s ease, border-color 0.2s ease, background 0.2s ease;
    }

    .startup-card:hover {
      transform: translateY(-4px);
      border-color: rgba(110,231,249,0.32);
      background: linear-gradient(180deg, rgba(127,183,255,0.13), rgba(255,255,255,0.052));
    }

    .startup-card.hidden { display:none; }

    .startup-top {
      display:flex;
      justify-content:space-between;
      gap: 12px;
      align-items:flex-start;
    }

    .startup-name {
      font-size: 20px;
      line-height: 1.05;
      font-weight: 950;
      letter-spacing: -0.045em;
    }

    .tagline {
      color: var(--muted);
      line-height:1.35;
      font-size:13px;
      margin-top: 7px;
    }

    .badge {
      display:inline-flex;
      align-items:center;
      justify-content:center;
      border-radius:999px;
      padding: 6px 9px;
      font-size: 11px;
      font-weight: 850;
      line-height:1;
      color: #061022;
      white-space:nowrap;
    }

    .badge.smart { background: var(--blue); }
    .badge.data { background: var(--purple); }
    .badge.help { background: var(--green); }
    .badge.env { background: var(--yellow); }
    .badge.fin { background: var(--orange); }

    .mini-row {
      display:grid;
      grid-template-columns: 1fr 1fr;
      gap: 10px;
    }

    .mini-box {
      border: 1px solid rgba(255,255,255,0.09);
      background: rgba(0,0,0,0.12);
      border-radius: 16px;
      padding: 12px;
    }

    .mini-label { color: var(--muted-2); font-size: 10px; text-transform:uppercase; letter-spacing:0.07em; font-weight:850; }
    .mini-value { margin-top: 5px; font-weight:900; font-size: 16px; }

    .score-bars { display:grid; gap: 8px; margin-top:auto; }
    .score-line { display:grid; grid-template-columns: 74px 1fr 24px; gap: 8px; align-items:center; }
    .score-line span { color: var(--muted); font-size: 11px; }
    .bar { height: 8px; background: rgba(255,255,255,0.10); border-radius: 99px; overflow:hidden; }
    .fill { height: 100%; border-radius: 99px; background: linear-gradient(90deg, var(--cyan), var(--green)); }
    .fill.orange { background: linear-gradient(90deg, var(--yellow), var(--orange)); }
    .fill.purple { background: linear-gradient(90deg, var(--blue), var(--purple)); }
    .score-number { font-size: 11px; color: var(--muted); text-align:right; }

    .matrix-wrap {
      display:grid;
      grid-template-columns: minmax(0, 1fr) 360px;
      gap:16px;
      align-items: stretch;
    }

    .matrix {
      position:relative;
      min-height: 580px;
      border-radius: var(--radius);
      background:
        linear-gradient(90deg, rgba(255,255,255,0.07) 1px, transparent 1px),
        linear-gradient(180deg, rgba(255,255,255,0.07) 1px, transparent 1px),
        linear-gradient(135deg, rgba(255,255,255,0.05), rgba(255,255,255,0.025));
      background-size: 50% 100%, 100% 50%, 100% 100%;
      border: 1px solid rgba(255,255,255,0.11);
      overflow:hidden;
    }

    .axis-label {
      position:absolute;
      color: var(--muted-2);
      font-size: 12px;
      font-weight:800;
      text-transform:uppercase;
      letter-spacing:0.08em;
    }

    .axis-x { left:50%; bottom:14px; transform:translateX(-50%); }
    .axis-y { top:50%; left:14px; transform:rotate(-90deg) translateX(-50%); transform-origin:left center; }

    .quadrant-label {
      position:absolute;
      color: rgba(255,255,255,0.22);
      font-weight:950;
      font-size: clamp(22px, 3vw, 38px);
      letter-spacing:-0.06em;
      line-height:0.95;
      pointer-events:none;
    }

    .q1 { right:24px; top:22px; text-align:right; }
    .q2 { left:24px; top:22px; }
    .q3 { left:24px; bottom:42px; }
    .q4 { right:24px; bottom:42px; text-align:right; }

    .dot {
      position:absolute;
      transform: translate(-50%, -50%);
      border: 1px solid rgba(255,255,255,0.22);
      background: rgba(12,18,38,0.92);
      box-shadow: 0 12px 32px rgba(0,0,0,0.35);
      border-radius: 18px;
      padding: 10px 11px;
      min-width: 118px;
      cursor:pointer;
      transition:0.18s ease;
    }

    .dot:hover {
      transform: translate(-50%, -50%) scale(1.045);
      border-color: rgba(110,231,249,0.45);
    }

    .dot strong { display:block; font-size:12px; line-height:1.05; }
    .dot small { display:block; color: var(--muted); font-size:10px; margin-top:4px; }
    .dot::before {
      content:"";
      width:9px;
      height:9px;
      border-radius:50%;
      background: var(--cyan);
      display:inline-block;
      margin-right:6px;
      box-shadow: 0 0 20px rgba(110,231,249,0.7);
    }

    .dot.smart::before { background: var(--blue); box-shadow: 0 0 20px rgba(127,183,255,0.7); }
    .dot.data::before { background: var(--purple); box-shadow: 0 0 20px rgba(200,167,255,0.7); }
    .dot.help::before { background: var(--green); box-shadow: 0 0 20px rgba(124,242,179,0.7); }
    .dot.env::before { background: var(--yellow); box-shadow: 0 0 20px rgba(255,224,138,0.7); }
    .dot.fin::before { background: var(--orange); box-shadow: 0 0 20px rgba(255,177,105,0.7); }

    .legend {
      display:grid;
      gap: 10px;
    }

    .legend-item {
      display:flex;
      align-items:center;
      justify-content:space-between;
      gap:12px;
      border: 1px solid rgba(255,255,255,0.09);
      background: rgba(255,255,255,0.045);
      padding: 13px;
      border-radius: 16px;
      cursor:pointer;
      transition:0.18s ease;
    }

    .legend-item:hover { background: rgba(255,255,255,0.075); }
    .legend-name { font-weight:900; font-size:13px; }
    .legend-meta { color: var(--muted); font-size:11px; margin-top:3px; }
    .legend-score { color: var(--cyan); font-weight:950; font-size:18px; }

    .heatmap {
      overflow:auto;
      border-radius: 20px;
      border: 1px solid rgba(255,255,255,0.1);
    }

    table {
      width:100%;
      border-collapse:collapse;
      min-width: 920px;
      background: rgba(255,255,255,0.035);
    }

    th, td {
      text-align:left;
      padding: 14px 13px;
      border-bottom: 1px solid rgba(255,255,255,0.08);
      font-size: 13px;
      vertical-align: top;
    }

    th {
      color: var(--muted);
      font-size: 11px;
      text-transform:uppercase;
      letter-spacing:0.08em;
      font-weight:900;
      background: rgba(255,255,255,0.055);
      position: sticky;
      top:0;
      z-index:1;
    }

    td:first-child,
    th:first-child { position: sticky; left:0; background: #111936; z-index:2; }
    th:first-child { z-index:3; }

    .level {
      display:inline-flex;
      min-width: 72px;
      justify-content:center;
      padding: 6px 8px;
      border-radius: 999px;
      font-size: 11px;
      font-weight: 900;
      color:#061022;
    }
    .level.high { background: var(--green); }
    .level.med { background: var(--yellow); }
    .level.low { background: rgba(255,255,255,0.22); color: var(--text); }
    .level.none { background: rgba(255,255,255,0.08); color: var(--muted-2); }

    .stack-chain {
      display:grid;
      grid-template-columns: repeat(5, minmax(150px, 1fr));
      gap: 12px;
      margin-top: 18px;
    }

    .chain-step {
      position:relative;
      border: 1px solid rgba(255,255,255,0.11);
      background: rgba(255,255,255,0.055);
      border-radius: 20px;
      padding: 18px;
      min-height: 170px;
    }

    .chain-step:not(:last-child)::after {
      content:"→";
      position:absolute;
      right:-18px;
      top:50%;
      transform: translateY(-50%);
      color: rgba(255,255,255,0.28);
      font-size: 24px;
      font-weight: 900;
    }

    .chain-title { font-weight:950; letter-spacing:-0.03em; }
    .chain-copy { color: var(--muted); font-size:13px; line-height:1.45; margin-top:8px; }
    .chain-startups { display:flex; flex-wrap:wrap; gap: 7px; margin-top:14px; }
    .pill {
      border: 1px solid rgba(255,255,255,0.1);
      background: rgba(255,255,255,0.07);
      color: var(--text);
      border-radius: 999px;
      padding: 6px 8px;
      font-size: 11px;
      font-weight: 800;
    }

    .detail-layout {
      display:grid;
      grid-template-columns: 330px minmax(0, 1fr);
      gap: 16px;
      align-items:start;
    }

    .startup-list {
      display:grid;
      gap: 8px;
      position: sticky;
      top: 88px;
    }

    .startup-tab {
      text-align:left;
      border: 1px solid rgba(255,255,255,0.09);
      background: rgba(255,255,255,0.05);
      color: var(--muted);
      border-radius: 16px;
      padding: 13px;
      cursor:pointer;
      transition:0.18s ease;
      font-family: inherit;
    }

    .startup-tab strong { display:block; color: var(--text); font-size: 14px; }
    .startup-tab span { display:block; margin-top: 3px; font-size: 11px; color: var(--muted-2); }
    .startup-tab.active,
    .startup-tab:hover { background: rgba(127,183,255,0.13); border-color: rgba(127,183,255,0.28); }

    .deep-card {
      border: 1px solid rgba(255,255,255,0.11);
      background: linear-gradient(145deg, rgba(255,255,255,0.085), rgba(255,255,255,0.045));
      border-radius: var(--radius);
      padding: 26px;
      min-height: 620px;
    }

    .deep-header {
      display:flex;
      justify-content:space-between;
      align-items:flex-start;
      gap:18px;
      border-bottom: 1px solid rgba(255,255,255,0.09);
      padding-bottom: 18px;
      margin-bottom: 18px;
    }

    .deep-title { font-size: 36px; font-weight: 950; letter-spacing:-0.055em; line-height:1; }
    .deep-sub { color: var(--muted); margin-top:7px; line-height:1.4; max-width:680px; }

    .deep-grid {
      display:grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 14px;
    }

    .deep-box {
      border: 1px solid rgba(255,255,255,0.09);
      background: rgba(0,0,0,0.12);
      border-radius: 18px;
      padding: 16px;
    }

    .deep-box.full { grid-column: 1 / -1; }
    .box-title {
      display:flex;
      align-items:center;
      gap:8px;
      color: var(--cyan);
      font-size: 11px;
      text-transform:uppercase;
      letter-spacing:0.08em;
      font-weight:950;
      margin-bottom: 9px;
    }
    .box-text { color: var(--muted); line-height: 1.48; font-size: 13px; }
    .box-text strong { color: var(--text); }

    ul.clean {
      margin: 0;
      padding: 0;
      list-style:none;
      display:grid;
      gap:8px;
    }

    ul.clean li {
      color: var(--muted);
      line-height:1.42;
      font-size: 13px;
      position:relative;
      padding-left:18px;
    }
    ul.clean li::before {
      content:"";
      position:absolute;
      width:7px;
      height:7px;
      border-radius:50%;
      left:0;
      top:7px;
      background: var(--cyan);
    }

    .scorecard-table td:nth-child(2) { width: 170px; }
    .score-pill {
      display:inline-flex;
      align-items:center;
      gap:8px;
      font-weight:950;
      color: var(--text);
    }
    .score-dot {
      width: 10px;
      height:10px;
      border-radius:50%;
      background: var(--green);
      box-shadow: 0 0 18px rgba(124,242,179,0.55);
    }
    .score-dot.mid { background: var(--yellow); box-shadow: 0 0 18px rgba(255,224,138,0.55); }
    .score-dot.low { background: var(--red); box-shadow: 0 0 18px rgba(255,125,138,0.55); }

    .roadmap {
      display:grid;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 14px;
    }

    .roadmap-col {
      border: 1px solid rgba(255,255,255,0.10);
      background: rgba(255,255,255,0.055);
      border-radius: 22px;
      padding: 16px;
      min-height: 360px;
    }

    .roadmap-title {
      display:flex;
      justify-content:space-between;
      align-items:center;
      gap:10px;
      font-weight:950;
      letter-spacing:-0.035em;
      font-size:20px;
      margin-bottom: 12px;
    }

    .roadmap-tag {
      font-size:10px;
      padding:6px 8px;
      border-radius:999px;
      color:#061022;
      background: var(--cyan);
      font-weight:900;
      letter-spacing:0.05em;
      text-transform:uppercase;
    }

    .roadmap-card {
      border: 1px solid rgba(255,255,255,0.09);
      background: rgba(0,0,0,0.13);
      border-radius: 16px;
      padding: 13px;
      margin-top: 10px;
    }
    .roadmap-card strong { display:block; font-size:14px; }
    .roadmap-card span { display:block; color: var(--muted); font-size:12px; line-height:1.4; margin-top:5px; }

    .footer-note {
      margin-top: 32px;
      padding: 18px 0 0;
      border-top: 1px solid rgba(255,255,255,0.09);
      color: var(--muted-2);
      font-size: 12px;
      display:flex;
      justify-content:space-between;
      gap:20px;
      flex-wrap:wrap;
    }

    .tooltip {
      position: fixed;
      z-index: 200;
      pointer-events:none;
      max-width: 360px;
      padding: 13px 14px;
      border: 1px solid rgba(255,255,255,0.13);
      background: rgba(9, 14, 30, 0.94);
      backdrop-filter: blur(16px);
      border-radius: 16px;
      box-shadow: var(--shadow);
      opacity:0;
      transform: translateY(8px);
      transition: opacity 0.12s ease, transform 0.12s ease;
    }
    .tooltip.visible { opacity:1; transform: translateY(0); }
    .tooltip strong { display:block; margin-bottom:5px; }
    .tooltip span { color: var(--muted); font-size:12px; line-height:1.42; }

    @media (max-width: 1180px) {
      .hero { grid-template-columns: 1fr; }
      .startup-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
      .matrix-wrap { grid-template-columns: 1fr; }
      .detail-layout { grid-template-columns: 1fr; }
      .startup-list { position:relative; top:auto; grid-template-columns: repeat(2, minmax(0,1fr)); }
      .stack-chain { grid-template-columns: 1fr; }
      .chain-step:not(:last-child)::after { display:none; }
    }

    @media (max-width: 760px) {
      .shell, .topbar-inner { width: min(100% - 24px, 1440px); }
      .topbar-inner { align-items:flex-start; flex-direction:column; }
      .hero { padding-top: 38px; }
      .hero-panel { padding: 22px; }
      .hero-side { grid-template-columns: 1fr; }
      .metric-card.big { grid-column:auto; }
      .startup-grid, .grid.two, .grid.three, .grid.four, .roadmap, .deep-grid { grid-template-columns: 1fr; }
      .deep-box.full { grid-column:auto; }
      .startup-list { grid-template-columns: 1fr; }
      .section-head { flex-direction:column; align-items:flex-start; }
      .matrix { min-height: 640px; }
      .dot { min-width: 102px; padding: 9px; }
    }
  </style>
</head>
<body>
  <div class="topbar">
    <div class="topbar-inner">
      <a class="brand" href="#top" aria-label="Torna all'inizio">
        <div class="brand-mark">W</div>
        <div class="brand-title">
          <strong>WDA × PSN</strong>
          <span>Startup Scouting Decision Dashboard</span>
        </div>
      </a>
      <nav class="nav" id="nav">
        <a href="#overview">Overview</a>
        <a href="#matrix">Priorità POC</a>
        <a href="#coverage">Verticali</a>
        <a href="#stack">Data Value Chain</a>
        <a href="#startups">Startup</a>
        <a href="#roadmap">Roadmap</a>
      </nav>
    </div>
  </div>

  <main class="shell" id="top">
    <header class="hero">
      <div class="hero-panel">
        <div class="eyebrow">Riservato · Aprile 2026 · PSN Innovation Hub</div>
        <h1>Da scouting startup a decisione operativa.</h1>
        <p class="hero-copy">
          Una dashboard per tradurre il linguaggio startup in una vista leggibile per PSN: cosa fa ogni soluzione, perché è diversa, quanto è pronta per la PA e quale collaborazione avviare per prima.
        </p>
        <div class="hero-actions">
          <a class="primary-cta" href="#matrix">Vedi priorità POC</a>
          <a class="secondary-cta" href="#startups">Esplora le startup</a>
        </div>
      </div>

      <div class="hero-side" id="overview">
        <div class="metric-card big">
          <div>
            <div class="metric-value">8</div>
            <div class="metric-label">startup AI selezionate per verticali PSN</div>
          </div>
          <div class="metric-note">Smart Cities · Data Intelligence · Help Desk · Ambiente · FinOps</div>
        </div>
        <div class="metric-card">
          <div class="metric-value">5</div>
          <div class="metric-label">verticali strategici coperti</div>
        </div>
        <div class="metric-card">
          <div class="metric-value">4</div>
          <div class="metric-label">cluster decisionali per il follow-up</div>
        </div>
        <div class="metric-card">
          <div class="metric-value">3</div>
          <div class="metric-label">startup da portare subito a demo/POC</div>
        </div>
        <div class="metric-card">
          <div class="metric-value">1</div>
          <div class="metric-label">matrice unica per decidere le priorità</div>
        </div>
      </div>
    </header>

    <section>
      <div class="section-head">
        <div>
          <div class="section-kicker">Portfolio snapshot</div>
          <h2>Le startup non sono tutte nello stesso punto della curva.</h2>
          <p class="section-desc">La vista sintetica combina strategic fit con PSN, readiness PA, maturità prodotto e facilità di avvio POC. Non è un ranking assoluto: serve a decidere il prossimo passo giusto per ciascuna soluzione.</p>
        </div>
        <button class="button-like" onclick="window.print()">Esporta / stampa PDF</button>
      </div>

      <div class="filters" aria-label="Filtri verticali">
        <button class="filter-btn active" data-filter="all">Tutte</button>
        <button class="filter-btn" data-filter="Smart Cities">Smart Cities</button>
        <button class="filter-btn" data-filter="Data Intelligence">Data Intelligence</button>
        <button class="filter-btn" data-filter="Help Desk">Help Desk</button>
        <button class="filter-btn" data-filter="Ambiente">Ambiente</button>
        <button class="filter-btn" data-filter="FinOps">FinOps</button>
      </div>

      <div class="startup-grid" id="startupGrid"></div>
    </section>

    <section id="matrix">
      <div class="section-head">
        <div>
          <div class="section-kicker">Pilot priority matrix</div>
          <h2>Impatto potenziale × facilità di avvio POC.</h2>
          <p class="section-desc">La matrice separa le startup pronte per un POC rapido da quelle ad alto potenziale ma che richiedono maggiore progettazione tecnica, stakeholder dedicati o un contesto di sperimentazione più controllato.</p>
        </div>
      </div>
      <div class="matrix-wrap">
        <div class="matrix panel" id="priorityMatrix">
          <div class="quadrant-label q1">Start<br/>now</div>
          <div class="quadrant-label q2">Design<br/>carefully</div>
          <div class="quadrant-label q3">Monitor</div>
          <div class="quadrant-label q4">Quick<br/>experiment</div>
          <div class="axis-label axis-x">Facilità avvio POC →</div>
          <div class="axis-label axis-y">Impatto potenziale →</div>
        </div>
        <div class="panel legend" id="matrixLegend"></div>
      </div>
    </section>

    <section id="coverage">
      <div class="section-head">
        <div>
          <div class="section-kicker">Vertical coverage map</div>
          <h2>Dove ogni startup intercetta i verticali PSN.</h2>
          <p class="section-desc">La heatmap aiuta PSN a capire se la startup è una soluzione verticale, un layer trasversale o un abilitatore di più linee progettuali.</p>
        </div>
      </div>
      <div class="panel">
        <div class="heatmap">
          <table id="coverageTable"></table>
        </div>
      </div>
    </section>

    <section id="stack">
      <div class="section-head">
        <div>
          <div class="section-kicker">Data Intelligence logic</div>
          <h2>Non tutte le soluzioni competono: alcune compongono lo stack.</h2>
          <p class="section-desc">Per il cluster Data Intelligence, la lettura più utile è una value chain: prima si rende il dato utilizzabile, poi si estraggono insight, poi si passa all'azione e alla previsione.</p>
        </div>
      </div>
      <div class="panel">
        <div class="stack-chain">
          <div class="chain-step">
            <div class="chain-title">Dati frammentati</div>
            <div class="chain-copy">PDF, Excel, database, export gestionali e fonti non allineate.</div>
            <div class="chain-startups"><span class="pill">FEDRO</span></div>
          </div>
          <div class="chain-step">
            <div class="chain-title">Dati puliti</div>
            <div class="chain-copy">Normalizzazione, deduplicazione, ontologie e vista unificata.</div>
            <div class="chain-startups"><span class="pill">FEDRO</span></div>
          </div>
          <div class="chain-step">
            <div class="chain-title">Insight</div>
            <div class="chain-copy">Analisi semantica di feedback aperti e informazioni non strutturate.</div>
            <div class="chain-startups"><span class="pill">Surwiz</span></div>
          </div>
          <div class="chain-step">
            <div class="chain-title">Azione</div>
            <div class="chain-copy">Piani operativi, priorità di intervento e raccomandazioni leggibili.</div>
            <div class="chain-startups"><span class="pill">Surwiz</span></div>
          </div>
          <div class="chain-step">
            <div class="chain-title">Previsione</div>
            <div class="chain-copy">Forecasting zero-shot su serie temporali per budget, energia e domanda servizi.</div>
            <div class="chain-startups"><span class="pill">ContinualIST</span></div>
          </div>
        </div>
      </div>
    </section>

    <section id="startups">
      <div class="section-head">
        <div>
          <div class="section-kicker">Startup deep dive</div>
          <h2>Una scheda decisionale, non una brochure.</h2>
          <p class="section-desc">Ogni card risponde alle domande da due diligence: cosa fa, perché PSN dovrebbe interessarsene, qual è la differenza competitiva e quale POC avrebbe senso avviare.</p>
        </div>
      </div>
      <div class="detail-layout">
        <div class="startup-list" id="startupTabs"></div>
        <div class="deep-card" id="deepCard"></div>
      </div>
    </section>

    <section id="scorecard">
      <div class="section-head">
        <div>
          <div class="section-kicker">Due diligence lens</div>
          <h2>Score motivati: il voto conta meno della spiegazione.</h2>
          <p class="section-desc">La scorecard evita il rischio di trasformare startup molto diverse in un ranking artificiale. Ogni criterio deve aiutare PSN a capire cosa validare prima di collaborare.</p>
        </div>
      </div>
      <div class="panel">
        <div class="heatmap">
          <table class="scorecard-table" id="scorecardTable"></table>
        </div>
      </div>
    </section>

    <section id="roadmap">
      <div class="section-head">
        <div>
          <div class="section-kicker">Collaboration roadmap</div>
          <h2>Dal report alla prossima azione.</h2>
          <p class="section-desc">La roadmap traduce la due diligence in una pipeline di collaborazione: demo immediate, POC da progettare, startup da accompagnare o monitorare.</p>
        </div>
      </div>
      <div class="roadmap" id="roadmapGrid"></div>
    </section>

    <div class="footer-note">
      <span>WDA × PSN — Startup Scouting 2026 · Documento riservato</span>
      <span>Dashboard HTML single-file · pronta per export GitHub Pages</span>
    </div>
  </main>

  <div class="tooltip" id="tooltip"></div>

  <script>
    const verticalClass = {
      "Smart Cities": "smart",
      "Data Intelligence": "data",
      "Help Desk": "help",
      "Ambiente": "env",
      "FinOps": "fin"
    };

    const startups = [
      {
        name: "iotilize.me",
        vertical: "Smart Cities",
        secondary: ["Ambiente"],
        tagline: "Monitoraggio intelligente dei container per rifiuti industriali.",
        stage: "Pre-seed",
        location: "Roma",
        fit: 4,
        readiness: 4,
        maturity: 4,
        pocEase: 4,
        impact: 4,
        quadrant: "Quick / Start",
        x: 76,
        y: 72,
        what: "Installa sensori IoT su container industriali per mostrare posizione, livello di riempimento e necessità di svuotamento in tempo reale.",
        why: "Rilevante per Smart Cities e gestione rifiuti: riduce ritiri non necessari, abilita KPI ESG e si presta a POC con municipalizzate o centri di raccolta.",
        edge: "Differenza principale: monitoraggio volumetrico industriale plug&play, sensori proprietari, pricing a consumo e traction con operatori nazionali.",
        risk: "Serve validare scalabilità su enti pubblici diversi e integrazione con gestionali esistenti.",
        poc: "POC su centri di raccolta o municipalizzata: sensori installati, dashboard operativa, report su riduzione ritiri e logistica.",
        next: "Demo piattaforma + visita sito con sensori reali",
        competitors: "Alternative: tracking GPS semplice, gestione manuale svuotamenti, software rifiuti non sensor-based.",
        metrics: ["-30% costi logistici", "-23% ritiri non necessari", "±10% stima peso"]
      },
      {
        name: "FEDRO",
        vertical: "Data Intelligence",
        secondary: ["FinOps"],
        tagline: "Data Quality e Smart Data Integration per la PA italiana.",
        stage: "Pre-seed / MVP 2026",
        location: "Toscana",
        fit: 5,
        readiness: 2,
        maturity: 2,
        pocEase: 3,
        impact: 5,
        quadrant: "Design carefully",
        x: 43,
        y: 86,
        what: "Pulisce, allinea e rende interoperabili dati dispersi in PDF, Excel, database e gestionali pubblici senza sostituire i sistemi esistenti.",
        why: "È un abilitatore strutturale: senza qualità del dato, i progetti AI nella PA restano fragili o non scalabili.",
        edge: "Verticalizzazione sulla PA italiana, approccio layer sopra sistemi legacy, explainability e team con esperienza su AI applicata alla PA.",
        risk: "Readiness ancora early: MVP previsto e pricing non stabilizzato. Va validata su un flusso dati reale e circoscritto.",
        poc: "POC su dataset comunale frammentato: deduplicazione, ontologia, vista unificata e verifica umana delle eccezioni.",
        next: "Demo prototipo su dataset reale + workshop tecnico",
        competitors: "Alternative: Talend, Qlik, ETL enterprise, consulenza custom, operatori-ponte manuali.",
        metrics: [">90% accuratezza target", "Giorni vs mesi", "TRL 3-4"]
      },
      {
        name: "TALPA Inspection",
        vertical: "Ambiente",
        secondary: ["Smart Cities"],
        tagline: "Ispezione corrosione infrastrutture con drone, senza ponteggi.",
        stage: "Pre-seed / paid inspections",
        location: "Zurigo",
        fit: 4,
        readiness: 4,
        maturity: 4,
        pocEase: 2,
        impact: 5,
        quadrant: "Design carefully",
        x: 34,
        y: 86,
        what: "Rileva corrosione interna nel cemento armato tramite drone e sensore brevettato, generando dati geolocalizzati e digital twin.",
        why: "Rilevante per infrastrutture critiche: consente manutenzione preventiva su ponti, dighe e viadotti con dati oggettivi.",
        edge: "Tecnologia contactless brevettata, 10x più veloce di sistemi robotici, zero ponteggi e dataset proprietario per predictive maintenance.",
        risk: "POC meno immediato: richiede sito fisico, autorizzazioni, stakeholder infrastrutturali e validazione tecnica sul campo.",
        poc: "Ispezione dimostrativa su ponte o viadotto: mappa corrosione, digital twin e priorità manutentive.",
        next: "Demo tecnica dal vivo + accesso portale demo",
        competitors: "Alternative: ispezioni manuali, robot NDT, ponteggi, indagini visive, manutenzione reattiva.",
        metrics: [">100 m²/h", ">30% riduzione onsite", "13 ispezioni pagate"]
      },
      {
        name: "Surwiz",
        vertical: "Data Intelligence",
        secondary: ["Help Desk", "FinOps"],
        tagline: "Da feedback non strutturati a piani d'azione con AI generativa.",
        stage: "Post-accelerazione",
        location: "Italia",
        fit: 4,
        readiness: 4,
        maturity: 4,
        pocEase: 5,
        impact: 4,
        quadrant: "Start now",
        x: 86,
        y: 74,
        what: "Analizza risposte aperte, survey e feedback non strutturati per produrre priorità e piani d'azione operativi.",
        why: "La PA raccoglie molti feedback ma spesso non li trasforma in decisioni. Surwiz crea un ponte rapido tra ascolto e miglioramento servizi.",
        edge: "Non si ferma agli insight: produce azioni concrete. È più accessibile delle piattaforme enterprise e si configura via prompt.",
        risk: "Va verificata la profondità degli output su dataset PA complessi e la robustezza rispetto a domini istituzionali diversi.",
        poc: "Analisi di survey cittadini o consultazione pubblica: report operativo con priorità, temi critici e azioni raccomandate.",
        next: "Demo live su dataset reale + configurazione caso PA",
        competitors: "Alternative: Qualtrics, Medallia, Google Forms, analisi manuale, BI descrittiva.",
        metrics: ["€1.6M valutazione", "Personalizzazione in giorni", "Clienti: Iren, Widiba, Unipol"]
      },
      {
        name: "Act4City",
        vertical: "Help Desk",
        secondary: ["Smart Cities", "Data Intelligence"],
        tagline: "Assistente AI per la PA su WhatsApp, Telegram e telefono.",
        stage: "Early stage / seed",
        location: "Italia",
        fit: 5,
        readiness: 4,
        maturity: 4,
        pocEase: 5,
        impact: 5,
        quadrant: "Start now",
        x: 88,
        y: 88,
        what: "Risponde automaticamente alle domande dei cittadini sui servizi comunali, aggiornandosi da sito, documenti, ordinanze e API.",
        why: "Use case diretto per Help Desk PA: riduce carico front-office, estende servizio 24/7 e genera analytics sulle richieste dei cittadini.",
        edge: "AI-first per processi comunali, canali immediati, aggiornamento quotidiano e pricing accessibile anche per piccoli comuni.",
        risk: "Da validare qualità risposte su basi informative eterogenee e governance delle escalation verso operatori umani.",
        poc: "POC su 3-5 comuni: assistente multicanale, dashboard domande frequenti e misurazione riduzione richieste ripetitive.",
        next: "Demo live su comune reale + dashboard analytics",
        competitors: "Alternative: chatbot generici, FAQ statiche, call center, sportelli manuali, soluzioni custom di system integrator.",
        metrics: ["121 comuni contattati", "92.9% demo→proposta", "24/7"]
      },
      {
        name: "Heres / ASK4PA",
        vertical: "Help Desk",
        secondary: ["Data Intelligence"],
        tagline: "Piattaforma agentica modulare per la PA italiana.",
        stage: "Early growth / GTM",
        location: "Bologna",
        fit: 5,
        readiness: 4,
        maturity: 4,
        pocEase: 4,
        impact: 5,
        quadrant: "Start now",
        x: 78,
        y: 84,
        what: "Automatizza richieste cittadini, ticket, comunicazioni interne e supporto agli sportelli tramite moduli agentici specializzati.",
        why: "È una piattaforma più modulare e workflow-oriented per Help Desk PA, con potenziale estensione a sanità, ticketing e processi interni.",
        edge: "Architettura multi-agente, zero lock-in, model-agnostic, workflow su N8N open source, disponibilità su MePA.",
        risk: "Serve verificare complessità di deployment, qualità dei flussi custom e ownership operativa da parte dell'ente.",
        poc: "MVP su comuni pilota: modulo Orienta + Ticket, integrazione KB, dashboard KPI e definizione escalation.",
        next: "Demo tecnica approfondita + workshop PSN Innovation Hub",
        competitors: "Alternative: chatbot verticali, CRM/ticketing tradizionali, automazioni custom, call center.",
        metrics: ["ROI <18 mesi", "24/7", "MePA"]
      },
      {
        name: "Civimatica",
        vertical: "Smart Cities",
        secondary: ["Ambiente"],
        tagline: "Traffico urbano zero con gemelli digitali e AI edge.",
        stage: "Pre-seed",
        location: "Torino",
        fit: 4,
        readiness: 3,
        maturity: 3,
        pocEase: 3,
        impact: 5,
        quadrant: "Design carefully",
        x: 49,
        y: 80,
        what: "Usa dispositivi edge AI su autobus, semafori e punti urbani per costruire un digital twin della mobilità in tempo reale.",
        why: "Può abilitare gestione semafori, parcheggi, trasporto pubblico e simulazioni urbanistiche con approccio privacy-first.",
        edge: "Va oltre i dati GPS: interpreta il contesto stradale reale, processa on-device e propone un modello Device-as-a-Service zero CAPEX.",
        risk: "Tecnologia promettente ma da validare in campo con installazioni, integrazione urbana e metriche robuste di impatto.",
        poc: "Pilot su area urbana: device installati, digital twin, analisi traffico/parcheggi e simulazione di intervento.",
        next: "Demo digital twin Torino + presentazione device fisico",
        competitors: "Alternative: Google Maps/Waze, telecamere tradizionali, sensoristica urbana, piattaforme smart city enterprise.",
        metrics: ["-30% congestione target", "-25% CO2 target", "DaaS €200-500/mese"]
      },
      {
        name: "ContinualIST",
        vertical: "FinOps",
        secondary: ["Data Intelligence", "Ambiente"],
        tagline: "Forecasting zero-shot su qualsiasi dato temporale.",
        stage: "Early stage",
        location: "Italia / Pisa",
        fit: 4,
        readiness: 4,
        maturity: 4,
        pocEase: 4,
        impact: 5,
        quadrant: "Start now",
        x: 73,
        y: 82,
        what: "Credence è un foundation model per serie temporali che produce previsioni senza training specifico, storico lungo o data scientist dedicati.",
        why: "Può supportare FinOps PA, previsione consumi energetici, domanda servizi, anomalie di budget e capacity planning.",
        edge: "Zero-shot, zero training cost, benchmark indipendente forte, ricerca peer-reviewed e integrazione MCP-native per workflow agentici.",
        risk: "Serve tradurre la forza tecnica in casi PSN concreti e validare performance su dataset pubblici reali.",
        poc: "POC su dataset spesa, energia o domanda servizi: forecast probabilistico, alert e confronto con baseline tradizionali.",
        next: "Demo tecnica su dataset pubblico PA + workshop FinOps",
        competitors: "Alternative: modelli forecasting custom, AutoML, team data science interni, BI con previsioni semplici.",
        metrics: ["Zero training", "#1 GIFT-Eval", "ICLR 2026"]
      }
    ];

    const roadmap = [
      {
        title: "Start now",
        tag: "0–30 giorni",
        items: [
          ["Act4City", "Demo su comune reale, poi POC multicomune Help Desk."],
          ["Surwiz", "Test rapido su survey cittadini o feedback servizi digitali."],
          ["Heres / ASK4PA", "Workshop tecnico su moduli Orienta + Ticket e procurement MePA."],
          ["ContinualIST", "Benchmark su dataset PA time-series: energia, spesa o domanda servizi."]
        ]
      },
      {
        title: "Design carefully",
        tag: "30–60 giorni",
        items: [
          ["TALPA Inspection", "Serve asset fisico, autorizzazioni e stakeholder infrastrutturali."],
          ["Civimatica", "Pilot urbano da progettare con area, device, KPI e privacy assessment."],
          ["FEDRO", "POC circoscritto su dataset frammentato e obiettivo qualità chiaro."]
        ]
      },
      {
        title: "Scale / Monitor",
        tag: "60–120 giorni",
        items: [
          ["iotilize.me", "Da demo sensori a replicabilità su municipalizzate e centri raccolta."],
          ["FEDRO", "Se il POC conferma il valore, costruire standard data quality PA."],
          ["Civimatica", "Passare da use case mobilità singolo a digital twin urbano integrato."]
        ]
      }
    ];

    const criteria = [
      {
        name: "Strategic fit PSN",
        desc: "Quanto il problema affrontato è coerente con i verticali PSN e con bisogni strutturali della PA.",
        high: "Use case diretto e facilmente raccontabile a enti pubblici.",
        medium: "Fit chiaro ma da tradurre in uno scenario specifico.",
        low: "Rilevanza ancora da dimostrare o troppo indiretta."
      },
      {
        name: "PA readiness",
        desc: "Capacità di lavorare con vincoli pubblici: compliance, procurement, integrazione, linguaggio istituzionale.",
        high: "Già orientata alla PA o con barriere di adozione basse.",
        medium: "Serve adattamento del go-to-market o del deployment.",
        low: "Ancora lontana da requisiti e processi pubblici."
      },
      {
        name: "Defensibility",
        desc: "Qualità del vantaggio competitivo: IP, dati, architettura, execution, focus verticale.",
        high: "Differenziazione forte e difficilmente replicabile nel breve.",
        medium: "Differenza reale ma da validare contro alternative enterprise/custom.",
        low: "Rischio commodity o sostituibilità elevata."
      },
      {
        name: "Ease of POC",
        desc: "Quanto è semplice avviare una sperimentazione con dati, enti o asset disponibili.",
        high: "POC digitale, rapido, con output misurabile in poche settimane.",
        medium: "POC fattibile ma richiede preparazione o stakeholder multipli.",
        low: "POC complesso, fisico o dipendente da autorizzazioni."
      }
    ];

    function badgeClass(vertical) { return verticalClass[vertical] || "data"; }
    function scorePct(score) { return Math.max(0, Math.min(100, score * 20)); }
    function avgScore(s) { return Math.round(((s.fit + s.readiness + s.maturity + s.pocEase + s.impact) / 5) * 10) / 10; }

    function renderCards(filter = "all") {
      const grid = document.getElementById("startupGrid");
      grid.innerHTML = startups.map(s => {
        const visible = filter === "all" || s.vertical === filter || s.secondary.includes(filter);
        return `
          <article class="startup-card ${visible ? "" : "hidden"}" data-name="${s.name}">
            <div class="startup-top">
              <div>
                <div class="startup-name">${s.name}</div>
                <div class="tagline">${s.tagline}</div>
              </div>
              <span class="badge ${badgeClass(s.vertical)}">${s.vertical}</span>
            </div>
            <div class="mini-row">
              <div class="mini-box">
                <div class="mini-label">Stage</div>
                <div class="mini-value">${s.stage}</div>
              </div>
              <div class="mini-box">
                <div class="mini-label">Priority</div>
                <div class="mini-value">${avgScore(s)}/5</div>
              </div>
            </div>
            <div class="box-text">${s.why}</div>
            <div class="score-bars">
              <div class="score-line"><span>PSN fit</span><div class="bar"><div class="fill" style="width:${scorePct(s.fit)}%"></div></div><div class="score-number">${s.fit}</div></div>
              <div class="score-line"><span>Readiness</span><div class="bar"><div class="fill purple" style="width:${scorePct(s.readiness)}%"></div></div><div class="score-number">${s.readiness}</div></div>
              <div class="score-line"><span>POC ease</span><div class="bar"><div class="fill orange" style="width:${scorePct(s.pocEase)}%"></div></div><div class="score-number">${s.pocEase}</div></div>
            </div>
          </article>`;
      }).join("");

      document.querySelectorAll('.startup-card').forEach(card => {
        card.addEventListener('click', () => {
          const name = card.dataset.name;
          selectStartup(name);
          document.getElementById('startups').scrollIntoView({ behavior: 'smooth' });
        });
      });
    }

    function renderMatrix() {
      const matrix = document.getElementById("priorityMatrix");
      startups.forEach(s => {
        const el = document.createElement("div");
        el.className = `dot ${badgeClass(s.vertical)}`;
        el.style.left = `${s.x}%`;
        el.style.top = `${100 - s.y}%`;
        el.dataset.name = s.name;
        el.innerHTML = `<strong>${s.name}</strong><small>${s.quadrant}</small>`;
        el.addEventListener('click', () => {
          selectStartup(s.name);
          document.getElementById('startups').scrollIntoView({ behavior: 'smooth' });
        });
        attachTooltip(el, `<strong>${s.name}</strong><span>${s.poc}</span>`);
        matrix.appendChild(el);
      });

      const legend = document.getElementById("matrixLegend");
      legend.innerHTML = startups
        .slice()
        .sort((a,b) => avgScore(b) - avgScore(a))
        .map(s => `
          <div class="legend-item" data-name="${s.name}">
            <div>
              <div class="legend-name">${s.name}</div>
              <div class="legend-meta">${s.vertical} · ${s.quadrant}</div>
            </div>
            <div class="legend-score">${avgScore(s)}</div>
          </div>
        `).join("");

      document.querySelectorAll('.legend-item').forEach(item => {
        item.addEventListener('click', () => {
          selectStartup(item.dataset.name);
          document.getElementById('startups').scrollIntoView({ behavior: 'smooth' });
        });
      });
    }

    function levelCell(level) {
      const map = { "Alto":"high", "Medio":"med", "Basso":"low", "—":"none" };
      return `<span class="level ${map[level]}">${level}</span>`;
    }

    function coverageFor(s, vertical) {
      if (s.vertical === vertical) return "Alto";
      if (s.secondary.includes(vertical)) return "Medio";
      if (vertical === "Data Intelligence" && ["Act4City", "Civimatica"].includes(s.name)) return "Medio";
      if (vertical === "FinOps" && s.name === "Surwiz") return "Medio";
      return "—";
    }

    function renderCoverage() {
      const verticals = ["Smart Cities", "Data Intelligence", "Help Desk", "Ambiente", "FinOps"];
      const table = document.getElementById("coverageTable");
      table.innerHTML = `
        <thead><tr><th>Startup</th>${verticals.map(v => `<th>${v}</th>`).join("")}<th>Interpretazione</th></tr></thead>
        <tbody>
          ${startups.map(s => `
            <tr>
              <td><strong>${s.name}</strong><br/><span style="color:var(--muted);font-size:12px">${s.tagline}</span></td>
              ${verticals.map(v => `<td>${levelCell(coverageFor(s, v))}</td>`).join("")}
              <td style="color:var(--muted);line-height:1.42">${interpretation(s)}</td>
            </tr>
          `).join("")}
        </tbody>`;
    }

    function interpretation(s) {
      const map = {
        "iotilize.me": "Soluzione verticale su rifiuti e logistica urbana, con impatto ESG misurabile.",
        "FEDRO": "Layer abilitante per qualità dati: prerequisito di progetti AI e interoperabilità.",
        "TALPA Inspection": "Soluzione specialistica per manutenzione predittiva infrastrutturale.",
        "Surwiz": "Layer di trasformazione feedback → azione; utile su servizi cittadini e consultazioni.",
        "Act4City": "Help Desk PA immediatamente comprensibile, con forte potenziale multicomune.",
        "Heres / ASK4PA": "Piattaforma agentica modulare, più workflow-oriented del chatbot classico.",
        "Civimatica": "Smart city fisica: edge AI, mobilità e digital twin urbano.",
        "ContinualIST": "Forecasting trasversale per FinOps, energia e domanda servizi."
      };
      return map[s.name];
    }

    function renderTabs() {
      const tabs = document.getElementById("startupTabs");
      tabs.innerHTML = startups.map((s, idx) => `
        <button class="startup-tab ${idx === 0 ? "active" : ""}" data-name="${s.name}">
          <strong>${s.name}</strong>
          <span>${s.vertical} · ${s.stage}</span>
        </button>
      `).join("");
      document.querySelectorAll('.startup-tab').forEach(tab => {
        tab.addEventListener('click', () => selectStartup(tab.dataset.name));
      });
      selectStartup(startups[0].name, false);
    }

    function selectStartup(name, updateTabs = true) {
      const s = startups.find(x => x.name === name) || startups[0];
      if (updateTabs) {
        document.querySelectorAll('.startup-tab').forEach(tab => tab.classList.toggle('active', tab.dataset.name === s.name));
      }
      const deep = document.getElementById("deepCard");
      deep.innerHTML = `
        <div class="deep-header">
          <div>
            <div class="deep-title">${s.name}</div>
            <div class="deep-sub">${s.tagline}</div>
          </div>
          <span class="badge ${badgeClass(s.vertical)}">${s.vertical}</span>
        </div>
        <div class="deep-grid">
          <div class="deep-box">
            <div class="box-title">Cosa fa</div>
            <div class="box-text">${s.what}</div>
          </div>
          <div class="deep-box">
            <div class="box-title">Perché interessa a PSN</div>
            <div class="box-text">${s.why}</div>
          </div>
          <div class="deep-box">
            <div class="box-title">Vantaggio competitivo vero</div>
            <div class="box-text"><strong>${s.edge.split(':')[0]}</strong>${s.edge.includes(':') ? ':' + s.edge.split(':').slice(1).join(':') : s.edge}</div>
          </div>
          <div class="deep-box">
            <div class="box-title">Rischio da validare</div>
            <div class="box-text">${s.risk}</div>
          </div>
          <div class="deep-box full">
            <div class="box-title">POC consigliato</div>
            <div class="box-text">${s.poc}</div>
          </div>
          <div class="deep-box">
            <div class="box-title">Alternative / competitor indiretti</div>
            <div class="box-text">${s.competitors}</div>
          </div>
          <div class="deep-box">
            <div class="box-title">Metriche e segnali</div>
            <ul class="clean">${s.metrics.map(m => `<li>${m}</li>`).join("")}</ul>
          </div>
          <div class="deep-box full">
            <div class="box-title">Prossimo step</div>
            <div class="box-text"><strong>${s.next}</strong></div>
          </div>
        </div>`;
    }

    function scoreLabel(score) {
      if (score >= 4.2) return ['Alto', ''];
      if (score >= 3.2) return ['Medio', 'mid'];
      return ['Da validare', 'low'];
    }

    function renderScorecard() {
      const table = document.getElementById("scorecardTable");
      table.innerHTML = `
        <thead>
          <tr><th>Criterio</th><th>Lettura</th><th>Come usarlo nella due diligence PSN</th></tr>
        </thead>
        <tbody>
          ${criteria.map(c => `
            <tr>
              <td><strong>${c.name}</strong><br/><span style="color:var(--muted);font-size:12px;line-height:1.4">${c.desc}</span></td>
              <td>
                <div style="display:grid;gap:8px">
                  <span class="score-pill"><span class="score-dot"></span> Alto</span>
                  <span class="score-pill"><span class="score-dot mid"></span> Medio</span>
                  <span class="score-pill"><span class="score-dot low"></span> Da validare</span>
                </div>
              </td>
              <td style="color:var(--muted);line-height:1.5">
                <strong style="color:var(--text)">Alto:</strong> ${c.high}<br/>
                <strong style="color:var(--text)">Medio:</strong> ${c.medium}<br/>
                <strong style="color:var(--text)">Da validare:</strong> ${c.low}
              </td>
            </tr>
          `).join("")}
        </tbody>`;
    }

    function renderRoadmap() {
      const grid = document.getElementById("roadmapGrid");
      grid.innerHTML = roadmap.map(col => `
        <div class="roadmap-col">
          <div class="roadmap-title">${col.title}<span class="roadmap-tag">${col.tag}</span></div>
          ${col.items.map(([name, text]) => `
            <div class="roadmap-card" data-name="${name}">
              <strong>${name}</strong>
              <span>${text}</span>
            </div>
          `).join("")}
        </div>
      `).join("");
      document.querySelectorAll('.roadmap-card').forEach(card => {
        card.addEventListener('click', () => {
          selectStartup(card.dataset.name);
          document.getElementById('startups').scrollIntoView({ behavior: 'smooth' });
        });
      });
    }

    function setupFilters() {
      document.querySelectorAll('.filter-btn').forEach(btn => {
        btn.addEventListener('click', () => {
          document.querySelectorAll('.filter-btn').forEach(b => b.classList.remove('active'));
          btn.classList.add('active');
          renderCards(btn.dataset.filter);
        });
      });
    }

    function setupNavObserver() {
      const navLinks = Array.from(document.querySelectorAll('.nav a'));
      const sections = navLinks.map(a => document.querySelector(a.getAttribute('href'))).filter(Boolean);
      const observer = new IntersectionObserver(entries => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            navLinks.forEach(a => a.classList.toggle('active', a.getAttribute('href') === `#${entry.target.id}`));
          }
        });
      }, { rootMargin: '-35% 0px -55% 0px', threshold: 0.01 });
      sections.forEach(sec => observer.observe(sec));
    }

    function attachTooltip(el, html) {
      const tooltip = document.getElementById('tooltip');
      el.addEventListener('mouseenter', () => {
        tooltip.innerHTML = html;
        tooltip.classList.add('visible');
      });
      el.addEventListener('mousemove', e => {
        tooltip.style.left = `${Math.min(window.innerWidth - 380, e.clientX + 16)}px`;
        tooltip.style.top = `${e.clientY + 16}px`;
      });
      el.addEventListener('mouseleave', () => tooltip.classList.remove('visible'));
    }

    renderCards();
    renderMatrix();
    renderCoverage();
    renderTabs();
    renderScorecard();
    renderRoadmap();
    setupFilters();
    setupNavObserver();
  </script>
</body>
</html>
